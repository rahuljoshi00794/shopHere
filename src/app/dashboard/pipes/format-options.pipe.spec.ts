import { FormatOptionsPipe } from './format-options.pipe';

describe('FormatOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
